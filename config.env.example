# Copy to `config.env` and adjust values as needed.
#
# Docker (postgres container)
POSTGRES_DB=pos
POSTGRES_USER=pos
POSTGRES_PASSWORD=pos
POSTGRES_PORT=5433
#
# FastAPI (running on your host machine)
DB_HOST=localhost
DB_PORT=5433
DB_USER=pos
DB_PASSWORD=pos
DB_NAME=pos

