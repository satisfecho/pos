# HAProxy config for development: no HTTPS, no certs required.
# Used by base docker-compose.yml. Port 4202 for app, 80 for optional testing.
global
    log stdout format raw local0
    maxconn 4096

defaults
    log global
    mode http
    option httplog
    option dontlognull
    timeout connect 5000ms
    timeout client 50000ms
    timeout server 50000ms
    option forwardfor

frontend http_frontend
    bind *:80
    bind *:4202
    mode http
    # No HTTP→HTTPS redirect in dev; use http://localhost:4202 without certs

    acl is_app_ws path_beg /ws
    use_backend ws_backend if is_app_ws

    acl is_api path_beg /api
    use_backend api_backend if is_api

    default_backend frontend_backend

backend ws_backend
    mode http
    balance roundrobin
    option httpchk GET /health
    http-check expect status 200
    http-request set-path %[path,regsub(^/ws/,/)]
    server ws1 pos-ws-bridge:8021 check

backend api_backend
    mode http
    balance roundrobin
    option httpchk GET /docs
    http-check expect status 200
    http-request set-path %[path,regsub(^/api/,/)]
    server api1 pos-back:8020 check

backend frontend_backend
    mode http
    balance roundrobin
    option httpchk GET /
    http-check send hdr Host localhost
    http-check expect status 200
    http-request set-header Host localhost
    server front1 pos-front:80 check
