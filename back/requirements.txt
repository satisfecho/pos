fastapi>=0.110
uvicorn[standard]>=0.27

# ORM (Pydantic + SQLAlchemy)
sqlmodel>=0.0.22

# Postgres driver (SQLAlchemy "postgresql+psycopg://")
psycopg[binary]>=3.1

# Settings (already present in your venv, but make it explicit)
pydantic-settings>=2.0

# Auth
passlib[bcrypt]
bcrypt<4.1.0  # passlib incompatible with bcrypt>=4.1
python-jose[cryptography]
python-multipart